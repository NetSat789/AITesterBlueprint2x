# Context

- Initialized frontend (React+Vite) and backend (Node+Express) directories.
- Added Tailwind CSS manually.
- Built React layouts for the Main View (History sidebar, Chat Generator) and the Settings View (API configuration form).
- Connected the frontend forms to the express endpoints for unified configuration local storage.
- Integrated LLMs (Ollama, Groq, OpenAI) using OpenAI SDK wrapper and native fetch into the backend module.
- User successfully verified that the backend endpoint at `http://127.0.0.1:3001/api/config` and the frontend at `http://127.0.0.1:5173` are running properly.


